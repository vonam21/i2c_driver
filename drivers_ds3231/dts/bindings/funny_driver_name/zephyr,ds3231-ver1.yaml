description: |
    An example sensor that reads the GPIO level defined in input-gpios.

compatible: "zephyr,ds3231-ver1"
properties:
    address:
        type: int
        required: true
include: base.yaml